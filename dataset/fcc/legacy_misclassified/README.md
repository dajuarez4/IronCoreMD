# Historical misclassified archives

`2.29_4000K.npz` and `2.30_4000K.npz` were previously stored under FCC, but
their cell geometry is BCC-like. Identical copies already exist in
`dataset/bcc/non-mag/`. They are retained here only to preserve the history of
the earlier repository layout and are excluded from normal FCC selection.
