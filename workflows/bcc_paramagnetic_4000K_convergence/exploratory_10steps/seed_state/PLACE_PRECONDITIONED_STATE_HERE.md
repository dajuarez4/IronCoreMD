Copy the untouched `lambda=0.020` preconditioned QE state into this directory.

Required:

- `Fe_bcc2_pm_round4.save/`
- `Fe_bcc2_pm_round4.wfc1` through `Fe_bcc2_pm_round4.wfc40`

The batch script copies this protected seed into `md_state` before starting QE.
