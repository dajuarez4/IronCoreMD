# SC26 800-word summary

`summary.tex` uses the IEEE conference class and contains no references, as requested by the SC26 Research Posters instructions. Edit `metadata.tex` before submission.

Build with:

```bash
pdflatex summary.tex
pdflatex summary.tex
```

The repository machine did not have a TeX engine installed, so the included PDF is a faithful readable preview generated from the same text. Use the official IEEE LaTeX environment for the final submission PDF.
